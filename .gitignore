node_modules
dist
coverage
**/.DS_Store
.env*
!.env.template

# Cypress generated output (incl. generated Play Store assets)
cypress/screenshots
cypress/videos
cypress/downloads

# Android / Bubblewrap TWA build — generated project + signing secrets.
# twa-manifest.json is intentionally NOT ignored (it's the build config).
/android/
/app/
/build/
/.gradle/
/gradle/
gradlew
gradlew.bat
gradle.properties
build.gradle
settings.gradle
store_icon.png
manifest-checksum.txt
*.keystore
*.jks
*.aab
*.apk
*.apk.idsig
